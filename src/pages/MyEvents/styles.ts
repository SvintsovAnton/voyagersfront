import styled from "@emotion/styled"
import { colors } from "styles/colors"

export const MyEventsWrapper = styled.div`
  display: flex;
  flex: 1;
  background-color: ${colors.secondaryGrey};
  transition: all 0.2 ease;
`
